# idea

给出稀疏的医学图像，如CT图像，用diffusion model去补充缺失时间步的图像数据，然后做下游分析

针对每一个患者，不同的t有可能做了CT,也有可能没有做，所以需要根据每个患者的具体的数据补充其特定时间步

的缺失

| t      | 1 | 2 | 3 | 4 | ·· | T |
| ------ | - | - | - | - | ---- | - |
| 患者1  | 1 | 0 | 1 | 0 |      | 1 |
| 患者2  | 0 | 0 | 1 | 1 |      | 0 |
| 患者3  | 1 | 1 | 0 | 1 |      | 1 |
| ··· |   |   |   |   |      |   |
| 患者n  | 0 | 1 | 1 | 1 |      | 0 |

(表格中1代表有CT数据，0代表缺失CT数据)

## issue

但是目前DDPM是无条件生成的，也就是生成出的图片其实是完全独立的，而这里显然是要根据具体的患者的其他有数据的时间步的数据来生成缺失时间步的数据？

普通 DDPM：$x_t→x_0$

**我的目标：$x_T + 患者已有 CT + 时间 t + mask → 缺失时间点 CT$**

显然是不能用普通无条件 DDPM，普通 DDPM 只能学：所有 CT 图像长什么样，但是我想让模型知道同一个患者不同时间点之间如何变化。否则模型可能只是生成一张“看起来像 CT”的图，而不是这个患者特定时间点的 CT。

## how

做成“条件扩散模型用于纵向医学影像缺失时间点的概率性插补”，并不能用 DDPM 直接补图。**难点**在于条件设计、时间建模、缺失机制处理、医学验证，以及不能把生成图直接当成真实 CT。

# related paper

**Conditional Medical Image Generation WithDiffusion Models------------------------------**

条件扩散医学图像生成

**Med-cDiff** ，就是把 diffusion model 用于条件医学图像生成，任务包括 MRI 超分辨率、X-ray 去噪、MRI 图像到图像转换等。这个工作说明 conditional diffusion 在医学图像任务中是可行的

**ReMiND: Recovery of Missing Neuroimaging using Diffusion Models** -------------------------------

纵向医学影像生成

用 3D diffusion model 对缺失结构 MRI 进行插补，并且其在 whole-brain image imputation 上优于替代方法。和“缺失影像补全”逻辑非常接近，只是它主要是神经影像 MRI 场景。

SADM: Sequence-Aware Diffusion Model for Longitudinal Medical Image Generation------------------------

针对纵向医学影像生成，考虑序列长度不一、缺失数据、高维医学图像等问题，并用 sequence-aware transformer 作为扩散模型的条件模块来学习纵向依赖。

# Innovation？

生成图像是否能改善下游统计分析或预测模型？

影像组学特征提取
病灶体积变化趋势
治疗反应评估
预后预测
风险分层模型

能否证明补全后比“删掉缺失病例”或“简单插补”更稳定，这个就有临床分析价值。收窄到特定纵向 CT 场景，加入不规则时间、缺失机制、多重插补、不确定性传播和下游临床验证，就可以形成一个有论文价值的课题。

[innovation hit](https://chatgpt.com/share/6a28181a-8984-83ec-9331-0c45bb2a2585)

# how to train with sparse data?

训练时不能直接拿“本来就缺失”的样本监督。因为缺失时间点没有真值。所以训练时要做：

**伪缺失（masked training）---------------**

* 从一个本来**完整或相对完整**的患者时间序列里
* 选一个已有 CT 时间点当 target
* 假装把它遮掉
* 用剩下已知时间点作为条件
* 训练模型去恢复这个被遮掉的 target CT

这和 NLP 里的 masked modeling 很像。

① 条件怎么组织

最简单：最近前一张 + 最近后一张

以后可升级成：

* 最近 K 张
* 所有已知时间点 + transformer encoder
* 再加临床变量（年龄、治疗方案、lab 指标）

② 时间信息怎么进模型

dt_prev, dt_next 作为整张图的 constant map 拼进去。

以后可以升级成：

* clinical time embedding
* sinusoidal time encoding
* irregular time-aware transformer

③ 不确定性怎么体现

因为真正缺失时间点没有唯一真值，所以不要只生成一次。

推理时你应该：

* 换不同随机种子
* 生成多次
* 得到多张 plausible CT
* 做 **multiple imputation**

④	缺失机制怎么训练

要做  **伪缺失训练** ：

* 人为把本来已知的 target 时间点遮掉
* 用其余已知时间点恢复它

否则没有监督信号。

但我的是sparse data 

可以训练， **不需要每个患者都有完整时间序列** 。前面提到的“完整或相对完整序列”只是理想情况，不是硬性要求。真正需要的是： **训练时的 target 必须是某个真实存在的 CT** 。模型训练不能拿真正缺失的 0 当监督目标，但依然可以从已有的 1 里随机挑一个出来，假装它缺失。

训练时不是要求患者1、患者2、患者3都完整，而是这样构造训练样本：

```
患者1 已有 CT：t1, t3, tT
可以构造：
target = t1，condition = t3, tT
target = t3，condition = t1, tT
target = tT，condition = t1, t3
```

```
患者2 已有 CT：t3, t4
可以构造：
target = t3，condition = t4
target = t4，condition = t3
```

```
患者3 已有 CT：t1, t2, t4, tT
可以构造：
target = t1，condition = t2, t4, tT
target = t2，condition = t1, t4, tT
target = t4，condition = t1, t2, tT
target = tT，condition = t1, t2, t4
```

所以，只要一个患者至少有  **2 个时间点 CT** ，就可以构造“假缺失训练样本”。

训练目标是：给定同一患者其他已知时间点 CT，预测其中一个被人为遮掉的已知时间点 CT

如果患者只有1个时间步有CT,这就比较麻烦。那你无法训练“患者内部时间变化”，因为没有另一个时间点作为条件。但这种患者仍然可以用于学习：

```
CT 图像总体分布
器官形态分布
病灶外观分布
```

但不能很好地提供：同一患者随时间变化的监督信号，所以单时间点患者可以用于预训练，但对纵向条件插补帮助有限。

如果大部分患者都只有 1 个 CT，少数患者有 2 个以上，也可以做，但要谨慎。比较现实的路线是：

```
第一阶段：用所有 CT 做基础图像生成/去噪预训练
第二阶段：只用有多个时间点的患者做纵向条件 fine-tune
```

这样比直接从头训练条件模型稳。可以把训练集定义成：

```
对于患者 i，已观测时间集合为 O_i

O_i = {τ1, τ3, τ7}
```

训练时随机选：τ_target ∈ O_i 然后条件是：O_i \ {τ_target}

训练目标是：x_i,τ_target 模型输入是：

```
随机噪声 x_T
+ 患者其他已知 CT
+ 目标时间 τ_target
+ 已知时间点 τ_obs
+ 缺失 mask
```

模型输出：

x_i,τ_target 的噪声预测，这样训练目标仍然有 ground truth。

##### the 0 have value to model ?

真实缺失的 0 不能作为 target，但可以作为 **mask 信息** 输入模型。比如患者1：

```
t1 = 1
t2 = 0
t3 = 1
t4 = 0
t5 = 1
```

这个缺失模式本身可能有意义。因为患者为什么没拍 CT，可能和病情、治疗路径、失访、死亡风险、经济因素有关。

所以 mask 可以作为条件：[1, 0, 1, 0, 1],告诉模型：

```
这个患者哪些时间点有图，哪些时间点没有图
```

但注意：mask 只是条件，不是图像监督。

##### express in paper

由于真实纵向 CT 数据天然稀疏，模型训练不依赖完整时间序列。
对于每个患者已观测到的 CT 时间点集合，随机选择其中一个已观测时间点作为伪缺失目标，其余已观测时间点作为条件输入。模型学习在患者已有影像、随访时间编码和缺失 mask 条件下，重建被人为遮盖的目标时间点 CT。

##### caution

注意一个核心限制,这种训练学到的是：在“已有观测时间点之间”进行条件生成.

如果某些时间点从来没有多少真实 CT，比如 t8 几乎没人拍，那么模型很难学好 t8 的分布。

所以你需要统计每个时间点的样本量：

t1 有多少 CT
t2 有多少 CT
t3 有多少 CT
...
tT 有多少 CT

如果某个时间点真实样本极少，生成它的可信度就低。

##### apart 3 class

1. 插值 interpolation  目标时间点前后都有 CT。

已有 t1, t5
补 t3  这个最靠谱

2. 前向预测 extrapolation  只有目标之前的 CT。

已有 t1, t2
补 t4  这个更难，因为它涉及未来变化。

3. 反向估计 backward imputation  只有目标之后的 CT。

已有 t4, t5
补 t2  

这个也可以，但临床解释要小心。

论文里最好分别评估这三种场景，不要混在一起只报一个平均指标。

evaluate the  feasibility

先统计这几个指标：

```
1. 总患者数
2. 每个患者 CT 次数分布：1次、2次、3次、≥4次
3. 每个时间点 CT 覆盖率
4. 每个患者最大随访跨度
5. 相邻两次 CT 的时间间隔分布
6. 能构造多少个 pseudo-missing 训练样本
```

其中最关键的是： 能构造多少个 pseudo-missing 样本。如果一个患者有 m 个已观测 CT，那么理论上能构造 m 个 pseudo-target 样本：

# pre-weight?
